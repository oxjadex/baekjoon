#include <stdio.h>
int main () {
    int x1, x2, y1, y2, r1, r2;
    int x3, y3;
    int n, i, j, c; 
    scanf("%d", &n);
    while (i--) {
        scanf("%d %d %d %d %d %d", &x1, &y1, &r1, &r2, &x2, &y2);
        //i제곱 더하기 y제곱==r1인 경우 
    }
}